# 和音滇韵改版 Agent 执行计划

> 依据：`网站设计蓝图.md` v1.0（2026-08-18）  
> 计划状态：可执行，尚未开始业务代码改造  
> 目标：在保留 Astro、现有内容资产与 Three.js 照片宇宙的前提下，把网站升级为统一的纪实编辑叙事体验，并满足桌面、平板、移动端、无障碍与性能要求。

## 1. 不可偏离的产品约束

1. 全站正式名称只能是“和音滇韵”，副标题统一为“十四日音乐支教田野纪事”，品牌短句统一为“以音乐为桥，连接京滇山海”。
2. 视觉方向是“真实、温暖、克制、有节奏、有在场感”；禁止商业落地页、文旅海报、校园官网模板与炫技交互气质。
3. 全站唯一母题为“声纹—山路—时间线”；唯一强强调色为漆红 `#8C332D`。
4. 首页与纪事页使用米纸色，影像档案使用夜褐色；不扩展为全站深色。
5. 真实照片优先，不生成或使用虚构的纪实照片，不用正式合影作为首页首图。
6. 影像档案保留 Three.js 星图，并新增图册模式；移动端默认图册，用户主动选择后才加载星图。
7. 人员口径统一为“7 名学生成员＋2 名指导成员，共 9 人”。任何课程数、参与人数、成果数、授权状态不得推测。
8. 现有 11 篇日志全部保留；7 月 10 日和 7 月 14 日作为“序章/筹备”，7 月 26 日起进入十四日主时间轴，不伪造缺失日期的日志。
9. 所有动画都必须支持 `prefers-reduced-motion`；无动画时内容与功能保持完整。
10. 蓝图是设计与验收的唯一事实来源。实现建议与蓝图冲突时，以蓝图为准并记录偏差，不得静默改写目标。

## 2. 已探索的项目基线

- 技术栈：Astro 4、TypeScript、Three.js 0.185、原生 CSS、Vitest。
- 页面：`src/pages/index.astro`、`diary.astro`、`moments.astro`、`404.astro`。
- 共享层：`BaseLayout.astro`、`NavBar.astro`、`Footer.astro`、`global.css`。
- 内容资产：11 篇 Markdown 日志、35 条影像数据（12 条既有封面/合影记录＋23 条现场影像）、23 张 moments 图片、9 张成员头像。
- Three.js 已拆分为场景、渲染、相机、动画循环、交互、照片、特效与设备分级模块，适合渐进改造，不应重写。
- 当前基线：`npm test` 为 13 个测试文件、87 个测试全部通过；`npm run build` 成功。
- 已知构建告警：Three.js 客户端主包约 557 kB（gzip 约 146 kB）。改造后必须确保图册模式不加载该包，星图继续动态加载。
- 现状主要差距：亮橙色和圆角卡片体系、首页缺少叙事区块、日志仍为统一卡片列表、影像页无图册模式且移动端强制 Three.js、404 文案与图形不符合蓝图、人员口径不一致。

## 3. 开工前内容闸门

以下事项不阻塞布局与组件开发，但阻塞最终发布。协调者维护一张确认表，未确认内容用明确的“待提供”数据状态，不得写成面向访客的假数字或假授权。

| 项目 | 当前状态 | 处理规则 |
| --- | --- | --- |
| 首页主视觉 | 有现场照片可选，尚未确认最终图 | 从现有教学互动照片选 1 主 2 备；不得先用合影占位后忘记替换 |
| 三幕叙事图文 | 图片可从现有资产选择，80—120 字终稿未单独确认 | 先复用已核实日志摘要并标记内容复核，不杜撰活动 |
| 实践成果数字 | 蓝图未提供具体数字 | 数字区块采用数据驱动；无已核实数据时不展示具体数量 |
| 成员一句话描述 | 有完整履历，但未形成 25—45 字短描述 | 只从现有履历压缩，不新增经历或评价 |
| 照片故事长度 | 35 条数据齐全，部分少于 100 字 | 保留真实描述；内容补写需单独事实核验 |
| 肖像、姓名、声音授权 | 未在仓库记录 | 发布前人工确认；未确认儿童正面照改用背影、手部或共同动作照片 |
| 现场录音 | 仓库无音频 | 本轮不实现声音模块；后续有授权素材再作为独立任务加入 |
| Logo 素材 | `assets/logo.png` 存在，公开目录主要使用 JPG | 核查透明背景质量后再决定是否迁入 `public`，不做低质自动抠图 |

## 4. 多 Agent 工作规则

- 并发上限按 4 个槽位规划：1 名协调者＋最多 3 名执行 agent。不要让 4 个执行者同时修改共享工作区。
- 所有 agent 开工前先读完整蓝图与本计划，只修改任务卡声明的文件。发现必须越界时先通知协调者。
- `src/styles/global.css`、`BaseLayout.astro`、`NavBar.astro`、`Footer.astro` 仅由“共享基础 Agent”修改；其他 agent 只能消费其接口。
- `src/data/moments.ts` 和 `src/three/**` 仅由“影像 Agent”修改；`content/posts/**` 和 `PostTimeline.astro` 仅由“纪事 Agent”修改。
- Agent 不自行提交、不重置、不清理他人改动；协调者在每个波次验收后统一提交。严禁 `git reset --hard`、覆盖式 checkout 或删除不属于本任务的文件。
- 每名 agent 交付时必须报告：改动文件、实现摘要、测试命令与结果、未解决风险、是否触碰任务卡外文件。
- 一项失败不能通过跳过测试、降低无障碍、移除移动端功能或隐藏真实内容来“修复”。

## 5. 执行波次与依赖图

```text
Wave 0  协调者：内容盘点、冻结接口、建立基线
   │
   ▼
Wave 1  共享基础 Agent：设计令牌、布局、导航、页脚、通用交互语法
   │
   ├──────────────┬──────────────┐
   ▼              ▼              ▼
Wave 2A 首页 Agent  Wave 2B 纪事 Agent  Wave 2C 影像 Agent
   └──────────────┴──────────────┘
                  │
                  ▼
Wave 3  共享基础 Agent：404、跨页收口与接口修正
                  │
                  ▼
Wave 4  QA/协调者：视觉、响应式、无障碍、性能、回归与发布检查
```

只有 Wave 2A、2B、2C 可以并行。Wave 2 开始前必须通过 Wave 1 验收；Wave 3 不得在三个页面 agent 尚未交付时开始。

## 6. Wave 0：协调者基线与接口冻结

### 任务

- [ ] 执行 `git status --short`，记录并保护用户已有改动。
- [ ] 执行 `npm test` 与 `npm run build`，基线必须保持 87/87 测试通过和构建成功。
- [ ] 逐项核对 11 篇日志、35 条影像、9 名成员和对应图片是否存在。
- [ ] 从现有照片中给首页主视觉、三幕叙事、精选纪事、影像入口建立候选清单；只记录选择，不复制图片。
- [ ] 确认共享接口：页面主题 `paper/night`、是否沉浸 `immersive`、当前导航项、页脚显示策略、移动端断点。
- [ ] 冻结公共 CSS 命名与页面 agent 的文件所有权。

### 建议接口

- `BaseLayout` 增加轻量 props：`theme?: 'paper' | 'night'`、`immersive?: boolean`、`ogImage?: string`。不要引入客户端框架或状态库。
- 公共样式只提供设计令牌、网格、文字层级、档案标签、文本箭头链接、焦点态和 reduced-motion 基线；页面构图保留在页面或页面专属组件中。
- 共享组件只有在至少两个页面复用时才抽取，避免为蓝图中的每个名词创建组件。

### 完成标准

- 基线命令通过；无用户文件被覆盖；Wave 1 可在不猜测页面实现细节的情况下定义共享层。

## 7. Wave 1：共享基础 Agent

### 文件所有权

`src/styles/global.css`、`src/layouts/BaseLayout.astro`、`src/components/NavBar.astro`、`src/components/Footer.astro`，以及确有复用价值的新通用组件。此波次不修改四个页面和 Three.js 文件。

### 任务

- [ ] 将全站令牌更新为蓝图色板：米纸 `#F3EBDD`、纸白 `#FBF8F1`、墨褐 `#211B18`、漆红 `#8C332D`、竹金 `#C4954B`、雾灰 `#AFA79B`、夜褐 `#1D1714`。
- [ ] 建立 12/8/4 列响应式容器，最大宽度 1280px，桌面边距 48—64px、平板 32px、移动端 20px；统一主要区块间距与正文宽度。
- [ ] 建立标题宋体、正文黑体、日期等宽字体栈与蓝图字号层级；避免阻塞渲染的非必要字体请求，保留可靠系统回退。
- [ ] 重做导航：左侧文字标识＋单枚小 Logo，右侧“首页 / 十四日纪事 / 影像档案”，当前页为编号＋漆红短线；去掉胶囊背景与亮橙 hover。
- [ ] 导航支持 paper/night 反色、移动端单行、不拆品牌名、不平均挤压；在 320px 极窄宽度仍可使用，不用汉堡菜单掩盖三个主入口。
- [ ] 收束页脚为品牌句、学校/团队/年份和必要 GitHub 链接；避免重复大 Logo 与链接农场。
- [ ] 提供清晰焦点态、跳转主内容链接、触控目标、主题色 `color-scheme` 与 reduced-motion 全局基线。
- [ ] BaseLayout 正确处理沉浸页的页脚、主题 class、Open Graph 图片和 View Transition；不得让全局 `body` 样式被单页永久污染。

### 测试

- [ ] `npm test`
- [ ] `npm run build`
- [ ] 手工检查 `/`、`/diary/`、`/moments/`、`/404.html` 的导航激活状态与 GitHub Pages base path。

### 完成标准

- 共享页面在未改造页面内容的情况下仍可构建；导航在 320、375、768、1440px 不换行、不遮挡；全站已无亮橙主令牌。

## 8. Wave 2A：首页 Agent

### 文件所有权

`src/pages/index.astro`、首页专属新组件、首页专属数据文件。不得修改 `global.css`、导航、页脚、纪事、影像和 Three.js。

### 任务顺序

- [ ] 建立首页结构：首屏、项目宣言、实践概况、三幕叙事、精选纪事、实践成果、同行者、影像档案入口、收束页脚前文案。
- [ ] 首屏使用 5＋7 构图与真实互动照片，呈现唯一大标题“和音滇韵”、统一副标题、品牌短句、地点、日期与 `14 DAYS`；Logo 只作小印章。
- [ ] 用可访问的 CSS/SVG 线条实现一次“声纹—山路—时间线”连接，不复制民族纹样，不把它铺满背景。
- [ ] 项目宣言保持独立大留白，不放卡片、图标和按钮。
- [ ] 概况改为横向档案栏/谱线，移除 `InfoCard` 与 Emoji；团队口径改为 7＋2＝9。
- [ ] 三幕采用不等宽主图＋细节图和交替布局；内容只来自蓝图与已核实日志。
- [ ] 精选纪事选择初见、深入、回望三个阶段，一大两小，保留外部原文和“查看完整十四日纪事 →”。
- [ ] 实践成果采用数据数组；未核实数字时整个具体数字列表不渲染，不能展示 `0`、`--` 或虚构占位。
- [ ] 同行者分“指导成员 2 人 / 学生成员 7 人”，默认只显示姓名、职责、25—45 字短描述，完整履历使用原位展开（`details/summary` 或等价可键盘操作方案），取消九张同质白卡。
- [ ] 影像入口由米纸过渡到夜褐，展示 3—5 张漂浮但静态可读的真实照片，使用“进入十四日的回声”和“35 段影像”；链接进入 `/moments/`。
- [ ] 动画按蓝图节奏实现，支持 reduced-motion；图片声明尺寸或比例，首图优先加载，其余懒加载。

### 删除/退役

- [ ] 首页不再引用 `InfoCard.astro`；确认全仓无引用后由协调者决定是否删除。
- [ ] `TeamCard.astro` 若不再引用，不在本 agent 中删除，只报告给协调者。

### 测试与验收

- [ ] 首页只有一个首屏焦点，正式合影不在首屏；不存在 Emoji 信息卡、三等分卡片、20px 大圆角或宣传式渐变。
- [ ] 375、768、1440px 下阅读顺序一致；键盘可展开成员详情；关闭动画仍显示完整信息。
- [ ] `npm test`、`npm run build` 通过。

## 9. Wave 2B：十四日纪事 Agent

### 文件所有权

`src/pages/diary.astro`、`src/components/PostTimeline.astro`、`content/posts/**`，以及纪事专属数据/工具/测试。不得修改共享样式与其他页面。

### 任务顺序

- [ ] 扩展日志 frontmatter，至少支持 `location`、`phase`（prologue/journey）、可选 `featured`/`variant`；所有新增事实必须能从现有内容核对。
- [ ] 把数据解析与排序提取为可单测纯函数，处理无效日期、缺图、缺摘要和空列表，不依赖浏览器环境。
- [ ] 首屏以大型数字 `14`、标题“十四日纪事”、日期 `2026.07.26—08.08` 和蓝图引导语建立唯一焦点。
- [ ] 日期索引按已有记录生成：筹备记录归入“序章”，主时间轴从 7 月 26 日开始；索引支持锚点跳转、键盘操作与当前项标识。
- [ ] 桌面端使用左侧日期/地点/序号、右侧图片/标题/摘要/原文入口；重要节点大图、一般记录紧凑图文、文化专题图文对照，不强制等高。
- [ ] 用声纹时间线连接条目；IntersectionObserver 只负责渐进增强节点状态，关闭 JS 时仍可顺序阅读。
- [ ] 页面末尾加入“十四天之后，我们留下些什么”回望段落与影像档案入口。
- [ ] 移除卡片阴影、Emoji 占位和统一圆角卡片语言；缺图时使用档案线与文字说明，不使用通用音乐 Emoji。
- [ ] 外部微信链接保留 `target="_blank" rel="noopener noreferrer"` 并有明确“阅读微信原文”语义。

### 测试与验收

- [ ] 增加测试覆盖：日期排序、序章分组、无效 frontmatter 回退、锚点唯一性、11 篇日志不丢失。
- [ ] 索引与正文条目一一对应；无 JS、reduced-motion、键盘导航可用；移动端不出现横向页面溢出。
- [ ] `npm test`、`npm run build` 通过。

## 10. Wave 2C：影像档案 Agent

### 文件所有权

`src/pages/moments.astro`、`src/data/moments.ts`、`src/three/**`、影像专属新组件/工具/测试。不得修改全局共享文件与其他页面。

### 任务顺序

- [ ] 将页面标题与说明更新为“进入十四日的回声”“拖动探索 · 滚轮远近 · 点击照片查看故事”，并加入“星图 / 图册”模式切换。
- [ ] 桌面端默认星图；移动端/粗指针设备默认纵向摄影手记图册。移动端只有用户点击“进入星图”后才执行动态 import 和 `initUniverse`。
- [ ] 图册按日期稳定排序，保留横竖图自然比例，直接展示日期、地点和一句说明；详情可原位展开或侧边展开，不使用居中弹窗。
- [ ] 星图详情改为画面边缘侧栏，固定结构为日期、地点、标题、故事；关闭后恢复原视角与焦点，`Escape` 可关闭。
- [ ] 保留现有 SceneManager、PhotoManager、FocusController、设备分级和分阶段纹理加载；只做蓝图要求的增量修改，不重写渲染核心。
- [ ] 场景背景切换为夜褐 `#1D1714`，降低星点、雾气、光晕对比；漆红只用于当前照片、当前模式、日期节点和返回操作。
- [ ] reduced-motion 时停止持续漂浮/旋转或显著降低运动，并保证图册成为完整静态替代。
- [ ] 模式切换需释放监听器、动画循环、WebGL renderer、纹理与画布；重复进入退出不能创建多个实例。
- [ ] 35 条影像记录保持唯一 ID、有效路径、日期/地点/标题/说明；不因图册排序改变 Three.js 需要的数据契约。
- [ ] 避免 Three.js 进入图册首屏 bundle；构建后核对页面入口与动态 chunk，记录体积变化。

### 测试与验收

- [ ] 扩充数据测试：恰好 35 条、唯一 ID、合法日期、非空地点/标题/说明、资源路径存在或由构建可解析。
- [ ] 增加模式选择纯函数测试：窄屏/粗指针默认图册，桌面默认星图，用户选择优先。
- [ ] 保留并通过现有 Three.js 87 项基线相关测试；为资源释放或模式切换新增必要测试。
- [ ] 375px 首屏不初始化 WebGL；桌面星图拖动、缩放、点击、返回与 Escape 正常；重复切换无重复 canvas 或监听器。
- [ ] `npm test`、`npm run build` 通过，Three.js 仍按需加载。

## 11. Wave 3：404 与跨页收口 Agent

### 文件所有权

以 `src/pages/404.astro` 为主；共享文件只有在三个页面 agent 明确提出接口缺口并经协调者同意后才可修改。

### 任务

- [ ] 404 文案改为“这一段声音没有被记录。”“你来到了一页暂时不存在的记忆。”
- [ ] 提供首页、十四日纪事、影像档案三个明确方向，使用文字＋箭头，不使用胶囊按钮。
- [ ] 唯一图形为“中断后重新延续”的声纹线；移除音符漂浮动画和夸张 `404` 主焦点。
- [ ] 核对四页标题、导航名称、Meta 描述、OG 图、页脚、base path 和主题切换完全一致。
- [ ] 删除已确认无引用的旧组件与死样式；删除前必须用 `rg` 验证引用为零，且不得删除内容资产。

### 完成标准

- 404 与其他页面属于同一视觉系统；三个返回入口可键盘访问；全仓不存在旧导航名称“实践过程记录/精彩瞬间”作为主导航文案，不存在旧亮橙设计令牌的残留使用（历史文档除外）。

## 12. Wave 4：QA、验收与发布门槛

### 自动化检查

```powershell
git diff --check
npm test
npm run build
```

- [ ] 所有基线测试通过，新增测试通过，构建无错误。
- [ ] 不接受无说明的测试删除、`skip`、快照大面积重写或类型逃逸。
- [ ] 检查构建产物：图册默认路径不加载 Three.js；星图动态包没有异常增长。若主包仍超过 500 kB，记录原因与后续拆包建议，但不为消除警告破坏现有稳定模块。

### 页面矩阵

| 页面 | 320/375 | 768 | 1440 | 键盘 | reduced-motion | 无 JS 可读 |
| --- | --- | --- | --- | --- | --- | --- |
| 首页 | [ ] | [ ] | [ ] | [ ] | [ ] | [ ] |
| 十四日纪事 | [ ] | [ ] | [ ] | [ ] | [ ] | [ ] |
| 影像图册 | [ ] | [ ] | [ ] | [ ] | [ ] | [ ] |
| 影像星图 | 次级入口 | [ ] | [ ] | [ ] | [ ] | 图册替代 |
| 404 | [ ] | [ ] | [ ] | [ ] | [ ] | [ ] |

### 视觉与内容验收

- [ ] 每个首屏只有一个主焦点；全站正式名称、副标题、导航标签与人员口径一致。
- [ ] 米纸/夜褐、宋体/黑体/等宽日期、漆红状态、声纹线形成统一系统。
- [ ] 首页优先讲相遇与实践；三幕、精选纪事、同行者、影像高潮顺序完整。
- [ ] 纪事页能感到时间推进，11 条记录均可访问，不是重复卡片列表。
- [ ] 影像页桌面可探索、移动端可直接阅读，35 条影像信息完整。
- [ ] 不存在过量圆角、阴影、Emoji、民族装饰、持续运动、高饱和渐变或无意义动效。
- [ ] 图片 alt 不重复文件名；装饰线 `aria-hidden`；按钮、链接、详情展开和模式切换有可理解名称与可见焦点。
- [ ] 核实所有照片、人员、学校、地点、日期和数字；发布前完成肖像与声音授权人工签字确认。

### 性能建议门槛

- [ ] 首页首图不懒加载并设尺寸/比例；折叠线下图片懒加载。
- [ ] 移动端图册不初始化 WebGL；离开星图后动画循环停止、资源释放。
- [ ] 不新增大型 UI 框架、动画框架或客户端状态库。
- [ ] 在正常网络下避免字体成为首屏单点阻塞；系统字体回退不破坏布局。

## 13. 协调者集成顺序

1. Wave 1 交付后先验收共享接口并提交，之后才放行三个页面 agent。
2. Wave 2 并行期间用 `git diff --name-only` 监控文件所有权；出现重叠立即暂停相关 agent，不做自动覆盖合并。
3. 合并顺序为纪事 → 首页 → 影像；首页会消费日志链接，影像改动最大且最后进行运行时回归。
4. Wave 3 只做跨页一致性和死代码清理，不顺手重构 Three.js 或改写内容模型。
5. Wave 4 发现问题时回派给原文件 owner；共享样式问题回派共享基础 Agent，避免 QA agent 大范围越权修补。
6. 最终提交前再次对照蓝图第 21 节 12 条审美一致性标准逐项签字。

## 14. Agent 交付模板

每个 agent 完成任务时必须按以下格式返回：

```text
任务：Wave X / Agent 名称
状态：完成 / 部分完成 / 阻塞
改动文件：逐项列出
实现摘要：对应任务卡勾选项
验证：命令、结果、手工检查尺寸
未解决事项：内容缺口、技术风险、偏差及原因
越界修改：无 / 文件与协调者授权
```

## 15. 完成定义

只有同时满足以下条件才可宣布改版完成：代码与构建通过、四页完成蓝图结构、桌面与移动端均可用、影像双模式正确按需加载、无障碍与 reduced-motion 通过、11 篇日志和 35 条影像无丢失、9 人口径一致、所有真实数字与授权经人工确认、蓝图第 21 节审美验收全部通过。内容或授权闸门未关闭时，只能标记为“开发完成，待内容/授权发布审核”，不能直接发布。
