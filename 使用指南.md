# 使用指南 — 和音滇韵成果展示网页

本指南详细介绍如何修改网页上的每一项内容，以及修改后如何更新到线上。

---

## 目录

- [准备工作](#准备工作)
- [一、修改首页](#一修改首页)
  - [1.1 修改 Hero 区（大标题 + 副标题）](#11-修改-hero-区大标题--副标题)
  - [1.2 修改实践概况卡片](#12-修改实践概况卡片)
  - [1.3 修改"关于这次实践"正文](#13-修改关于这次实践正文)
  - [1.4 修改团队成员](#14-修改团队成员)
  - [1.5 更换首页 Hero 主图（团队合影）](#15-更换首页-hero-主图团队合影)
  - [1.6 添加团队成员头像](#16-添加团队成员头像)
- [二、修改实践日记页](#二修改实践日记页)
  - [2.1 新增一篇日记](#21-新增一篇日记)
  - [2.2 修改已有日记](#22-修改已有日记)
  - [2.3 在日记中插入图片](#23-在日记中插入图片)
  - [2.4 在日记中插入视频（外链）](#24-在日记中插入视频外链)
  - [2.5 删除一篇日记](#25-删除一篇日记)
- [三、修改图片视频页](#三修改图片视频页)
  - [3.1 理解画廊数据结构](#31-理解画廊数据结构)
  - [3.2 新增一张图片](#32-新增一张图片)
  - [3.3 替换已有图片](#33-替换已有图片)
  - [3.4 新增一个视频](#34-新增一个视频)
  - [3.5 修改注释文字](#35-修改注释文字)
  - [3.6 新增一天的媒体组](#36-新增一天的媒体组)
  - [3.7 图片和视频的两种引用方式](#37-图片和视频的两种引用方式)
- [四、修改页脚](#四修改页脚)
- [五、构建与推送](#五构建与推送)
  - [5.1 安装 Node.js（首次使用）](#51-安装-nodejs首次使用)
  - [5.2 克隆项目到本地（首次使用）](#52-克隆项目到本地首次使用)
  - [5.3 日常修改流程](#53-日常修改流程)
  - [5.4 完整命令速查](#54-完整命令速查)
- [六、常见问题](#六常见问题)

---

## 准备工作

### 你需要准备的工具

| 工具 | 用途 | 下载地址 |
|------|------|----------|
| **VS Code**（或其他编辑器） | 编辑 Markdown 和代码文件 | https://code.visualstudio.com |
| **Node.js** v20+ | 构建网页的命令行工具 | https://nodejs.org（下载 LTS 版本） |
| **Git** | 推送代码到 GitHub | https://git-scm.com |

> 如果你的电脑是 Windows，建议下载 Git for Windows（自带 Git Bash 终端）。本指南所有命令都在 Git Bash 中运行。

### 验证环境

安装完成后，打开终端（Git Bash），分别运行下面三条命令，看到版本号就说明安装成功：

```bash
node --version    # 应显示 v20.x.x 或更高
npm --version     # 应显示 10.x.x 或更高
git --version     # 应显示 2.x.x 或更高
```

### 首次克隆项目

```bash
# 克隆到本地
git clone https://github.com/Finderlzy/DianRhyme.git
cd DianRhyme

# 安装依赖（只需执行一次）
npm install
```

---

## 一、修改首页

首页由两部分组成：**Hero 区 + 概况卡片 + 关于实践正文 + 团队成员**。其中 Hero 区、概况卡片和团队成员写在代码文件 `src/pages/index.astro` 中，关于实践的正文写在 Markdown 文件 `content/overview.md` 中。

> ⚠️ **重要区分**：首页内容分散在两个文件中：
> - `src/pages/index.astro` → Hero 区、概况卡片、团队成员
> - `content/overview.md` → "关于这次实践"的正文段落

---

### 1.1 修改 Hero 区（大标题 + 副标题）

**文件位置**：`src/pages/index.astro`

**页面上的位置**：打开首页，第一眼看到的大字区域。

```
┌──────────────────────────────────┐
│  北京科技大学 · 酉良实践团        ← hero-eyebrow
│                                  │
│  和音滇韵                        ← hero-title (大标题)
│                                  │
│  以音乐为桥，连接京滇山海……       ← hero-subtitle (副标题)
│                                  │
│            ┌──────────┐         │
│            │  主图     │         ← 团队合影
│            └──────────┘         │
└──────────────────────────────────┘
```

**操作步骤**：

1. 用 VS Code 打开 `src/pages/index.astro`
2. 找到以下三行（约在第 29-31 行），修改对应文字：

```astro
<!-- 修改这三行 -->
<p class="hero-eyebrow">北京科技大学 · 酉良实践团</p>      ← 改这行：顶部小字
<h1 class="hero-title">和音滇韵</h1>                      ← 改这行：大标题
<p class="hero-subtitle">以音乐为桥，连接京滇山海。十二个日夜...</p>  ← 改这行：副标题
```

3. 保存文件

**改后效果**：刷新页面，Hero 区的文字直接变化。不需要动其他任何文件。

---

### 1.2 修改实践概况卡片

**文件位置**：`src/pages/index.astro`

**页面上的位置**：首页 Hero 下方，四个卡片组成的 2×2 网格。

```
┌─────────────┐ ┌─────────────┐
│ 📍 实践地点  │ │ 📅 实践时间  │
│ 澜沧拉祜族.. │ │ 7月27日-8月7│
└─────────────┘ └─────────────┘
┌─────────────┐ ┌─────────────┐
│ 👥 实践团队  │ │ 🎵 实践内容  │
│ 7名成员     │ │ 音乐教学... │
└─────────────┘ └─────────────┘
```

**操作步骤**：

1. 打开 `src/pages/index.astro`
2. 找到 `const infoItems = [...]` 这个数组（约在第 6-11 行）：

```js
const infoItems = [
  { icon: '📍', label: '实践地点', value: '云南省普洱市澜沧拉祜族自治县' },
  { icon: '📅', label: '实践时间', value: '2026年7月27日 — 8月7日 · 共12天' },
  { icon: '👥', label: '实践团队', value: '北科大"酉良和音滇韵"实践团 · 7名成员' },
  { icon: '🎵', label: '实践内容', value: '音乐基础教学 + 合唱排练 + 民族文化交融' },
];
```

3. 修改你想要的：
   - `icon` → 改成其他 emoji，比如 🏫 🎹 🎼
   - `label` → 卡片的标签文字
   - `value` → 卡片的具体内容

   > 比如想把地点改成更详细的地址，只修改 `value` 后面的文字即可：
   > ```js
   > { icon: '📍', label: '实践地点', value: '云南省普洱市澜沧拉祜族自治县 · 澜沧县特殊教育学校' },
   > ```

4. 保存文件

**改后效果**：刷新页面，对应卡片的内容立即更新。

---

### 1.3 修改"关于这次实践"正文

**文件位置**：`content/overview.md`

**页面上的位置**：首页中部，灰白色背景区域的大段文字。

**操作步骤**：

1. 打开 `content/overview.md`
2. 你会看到这样的内容：

```markdown
# 和音滇韵

## 北京科技大学音乐支教实践团

2026 年盛夏，我们——北科大"酉良和音滇韵"实践团的 7 名成员……
```

3. **直接改写文字**，就像平时写文档一样。比如：
   - 把 `7 名` 改成 `8 名`
   - 把某段话整段替换
   - 在末尾加一段新的总结

4. 保存文件

**Markdown 语法快速参考**（用于写日记和概况）：

| 你写 | 页面上显示 |
|------|-----------|
| `**加粗**` | **加粗** |
| `*斜体*` | *斜体* |
| `> 引用` | 灰色引用块（日记里的署名就用这个） |
| 空一行 = 新段落 | 两段之间有间距 |
| `### 小标题` | 三级标题 |

**改后效果**：构建后，"关于这次实践"区域的文字全部更新为你写的内容。

---

### 1.4 修改团队成员

**文件位置**：`src/pages/index.astro`

**页面上的位置**：首页最下方，团队成员卡片区域。

**操作步骤**：

1. 打开 `src/pages/index.astro`
2. 找到 `const teamMembers = [...]`（约在第 13-22 行）：

```js
const teamMembers = [
  { name: '雷梓宸', role: '队长 · 统筹安排 · 审核' },
  { name: '杜雅芬', role: '文字撰写 · 排版 · 教学' },
  // ... 其余成员
];
```

3. 每条记录包含：
   - `name` → 显示在卡片上的名字
   - `role` → 名字下方的职责描述

**场景一：修改某人的职责**

找到对应的人，改 `role` 后面的文字：

```js
{ name: '李昭琰', role: '技术支撑 · 摄影摄像 · 网页开发 · 教学' },
// 加上了" · 教学"
```

**场景二：新增一个成员**

在数组里加一行（注意不要漏掉逗号）：

```js
const teamMembers = [
  { name: '雷梓宸', role: '队长 · 统筹安排 · 审核' },
  // ... 前面的人不动
  { name: '新成员', role: '教学组 · 教授xx' },   // ← 新增这行，注意前面有逗号
];
```

**场景三：删除一个成员**

删掉对应那整行即可。

**改后效果**：卡片区域增减或更新对应成员信息。卡片上显示的是名字的后两个字（如"昭琰"），这是自动生成的，你不需要管。

---

### 1.5 更换首页 Hero 主图（团队合影）

**文件位置**：`src/pages/index.astro`（改代码） + `public/images/`（放图片）

**当前状态**：首页右侧显示的是一个虚线框占位符，没有真实图片。

**操作步骤**：

**第一步：准备图片**

1. 选择一张团队合影，建议尺寸 1200×900 左右
2. 把图片文件重命名为 `hero-team.jpg`（或其他你喜欢的名字）
3. 复制到 `public/images/` 文件夹

**第二步：修改代码**

1. 打开 `src/pages/index.astro`
2. 找到 Hero 区的图片占位符（约在第 34-38 行）：

```astro
<!-- 删除这整个占位块 -->
<div class="hero-placeholder">
  <span class="placeholder-icon">🎵</span>
  <span class="placeholder-text">团队合影 / 实践照片</span>
</div>
```

3. 替换为真实图片：

```astro
<!-- 替换为 -->
<img src="/DianRhyme/images/hero-team.jpg" alt="酉良和音滇韵实践团合影" class="hero-img" />
```

> ⚠️ **注意**：`src` 路径必须写 `/DianRhyme/images/...`，不能写 `/images/...`。因为网站部署在 `finderlzy.github.io/DianRhyme/` 子路径下，不以 `/DianRhyme` 开头的路径在线上会找不到图片。

4. 保存文件

**改后效果**：Hero 区占位符被真实的团队合影替代。如果你保留占位符在旁边先不删，可以对比确认后再删。

---

### 1.6 添加团队成员头像

**当前状态**：成员卡片折叠时显示彩色圆圈 + 名字后两个字（如"梓宸"）；鼠标悬停展开后，中部会显示一张 96×120 的**圆角真人照片**。没设置照片的成员展开时显示渐变色竖长占位。

**操作步骤**：

1. 准备好每个成员的照片（建议源图 ≥ 400×400，正脸、任意宽高比均可，会自动裁剪）
2. 放入 `public/images/avatars/` 文件夹，按拼音命名，如 `leizichen.jpg`
3. 打开 `src/pages/index.astro`，找到 `teamRows` 数组
4. 给每位成员填上 `avatar` 字段：

```js
{ name: '雷梓宸', role: '队长 · 小提琴', avatar: '/DianRhyme/images/avatars/leizichen.jpg' },
```

> ⚠️ **注意**：`avatar` 路径必须以 `/DianRhyme/images/avatars/` 开头（网站部署在子路径下，不能写 `/images/...`）。

5. 保存

**改后效果**：悬停卡片时，中部显示该成员的圆角真人照片。有 `avatar` 字段的显示照片，没有的显示渐变色方形占位。折叠状态始终显示彩色圆圈 + 名字缩写，不受影响。

---

## 二、修改实践日记页

实践日记页面是一个**可展开的时间线列表**。点击某一天 → 标题移到中央 → 展开正文（含文字、图片、视频）→ 其他条目下沉。再点一下或点"收起" → 内容折叠。

每篇日记对应 `content/diaries/` 文件夹下的一个 `.md` 文件。

### 日记文件的格式

打开 `content/diaries/day0.md`，你会看到它分两部分：

```markdown
---
day: 0                       ← 第几天（数字）
date: 2026-07-27             ← 日期（YYYY-MM-DD 格式）
title: 落地澜沧               ← 标题，显示在时间线上
---

这里开始是正文……
支持 Markdown 语法。
```

- `---` 之间的部分叫 **frontmatter**（元数据），告诉网页这篇日记的基本信息
- `---` 之后的部分是**正文**，会展开显示在时间线里

> 🚨 **关键**：frontmatter 里的三个字段（`day`、`date`、`title`）缺一不可，否则构建会报错。

---

### 2.1 新增一篇日记

假设你要新增 Day 2 的日记。

**第一步：创建文件**

在 `content/diaries/` 下新建文件 `day2.md`。

**第二步：填写内容**

```markdown
---
day: 2
date: 2026-07-29
title: 简谱初体验
---

今天是第三天教学。上午我们正式开始教简谱识读。

孩子们学得很快，有几个小朋友已经能自己用简谱唱出《小星星》了。

下午的训练中，我们开始分声部——女高、女低、男声三个声部。一开始大家不太习惯听别人的声音，总是被别的声部带跑调。练了三四遍之后，终于有了和声的感觉。那一刻，大家都笑了。

> 文字：杜雅芬 | 审核：雷梓宸
```

**第三步：保存**

**改后效果**：构建后，实践日记页面的时间线上会在 Day 1 下面多出一条"Day 2 · 简谱初体验"。点击后展开你写的正文。**不需要改任何代码，只需要创建 .md 文件即可。**

页面按 `day` 数字从小到大排列，所以你填 `day: 2` 它就自动排在 Day 1 后面。

---

### 2.2 修改已有日记

**操作步骤**：

1. 打开要改的 `.md` 文件，比如 `content/diaries/day1.md`
2. 直接修改：
   - 想改标题 → 改 `title:` 后面的文字
   - 想改日期 → 改 `date:` 后面的文字
   - 想改正文 → 改 `---` 下面的段落
3. 保存

**改后效果**：对应日记的标题、日期、正文在你构建后更新。

---

### 2.3 在日记中插入图片

**第一步：放图片**

把图片复制到 `public/images/` 文件夹，假设文件名为 `day2-music-class.jpg`。

**第二步：在日记中引用**

在 .md 文件的正文中，用 Markdown 语法插入图片：

```markdown
上午我们正式开始教简谱识读。

![孩子们学简谱](/DianRhyme/images/day2-music-class.jpg)

孩子们学得很快……
```

> ⚠️ **路径必须写 `/DianRhyme/images/...`**，和首页 Hero 图片同理，因为网站部署在子路径下。

**第三步：保存**

**改后效果**：展开这篇日记时，图片会出现在你插入的位置。图片自适应宽度，不会超出内容区。

---

### 2.4 在日记中插入视频（外链）

日记里不建议放本地视频文件（太大，加载慢）。推荐把视频上传到 **B站**（bilibili.com），然后用 iframe 嵌入。

**第一步：上传到 B站**

在 B站上传视频 → 审核通过后 → 复制"分享"里的嵌入代码。

**第二步：粘贴到日记中**

```markdown
下午训练时我们录了一段合唱练习。

<iframe src="//player.bilibili.com/player.html?bvid=BV1xx411x7xx" 
  scrolling="no" border="0" frameborder="no" framespacing="0" allowfullscreen="true"
  style="width:100%; aspect-ratio:16/9; border-radius:8px;">
</iframe>

看到孩子们认真的样子，真的很感动。
```

**改后效果**：展开日记后，B站播放器嵌入在正文中，可以直接点击播放。

---

### 2.5 删除一篇日记

直接删除对应的 `.md` 文件即可。不需要改任何代码。

**改后效果**：该篇日记从时间线上消失。

---

## 三、修改图片视频页

图片视频页是一个**三列时间线画廊**：

```
┌──────────┐  ┌──────────────┐  ┌─────────────────┐
│ 时间线    │  │  图片/视频    │  │  注释（hover展开） │
│ (左列)   │  │  (中列)       │  │  (右列)          │
└──────────┘  └──────────────┘  └─────────────────┘
```

所有画廊数据写在一个文件中：**`src/pages/gallery.astro`**。

---

### 3.1 理解画廊数据结构

打开 `src/pages/gallery.astro`，你会看到这样的结构：

```js
const galleryGroups = [
  {                          // ← 这是一个"日期组"
    date: '7月27日',          // 日期标签，显示在时间线节点旁
    items: [                 // 这个日期下的所有媒体条目
      {                      // ← 这是一个"条目"
        type: 'image',       // image = 图片, video = 视频
        src: base + 'images/day0-arrival.jpg',  // 文件路径
        caption: '抵达澜沧拉祜族自治县',          // 点击放大后显示的文字
        note: '抵达澜沧，县团委领导热情迎接了我们……',  // 右侧注释
      },
      // 同一天可以有多个条目……
    ],
  },
  {
    date: '7月28日',          // 第二个日期组
    items: [ ... ],
  },
];
```

层级关系：**`galleryGroups` → 日期组 → 条目（图片/视频）**

---

### 3.2 新增一张图片

**第一步：放图片**

把图片复制到 `public/images/`，假设文件名为 `day2-singing.jpg`。

**第二步：在对应日期组下添加条目**

打开 `src/pages/gallery.astro`，找到对应日期组（比如 `7月29日`），在它的 `items` 数组里加一条：

```js
{
  date: '7月29日',
  items: [
    // 已有条目不动……
    {
      type: 'image',
      src: base + 'images/day2-singing.jpg',        // ← 图片文件名
      caption: '孩子们第一次完整合唱《萱草花》',       // ← 点开放大后底部显示的文字
      note: '练了整整两天，当孩子们第一次完整唱出《萱草花》时，我们都有点想哭。', // ← 右侧注释
    },
  ],
},
```

> 💡 `base` 是自动替换为 `/DianRhyme/` 的变量，不需要手动改它。你只需要写 `base + 'images/文件名'`。

**第三步：保存**

**改后效果**：在对应日期的时间线节点下，出现新图片缩略图。鼠标悬停右侧注释→气泡展开；点击图片→Lightbox 放大查看。

---

### 3.3 替换已有图片

**方法一：同名覆盖（推荐，最简单）**

把新图片重命名为和旧图片完全一样的名字，放到 `public/images/` 覆盖原文件。**代码完全不用动。**

> 比如原来有一张 `day0-arrival.jpg`，你把新照片也命名为 `day0-arrival.jpg` 放进去覆盖。构建后图片自动更新。

**方法二：重命名 + 改引用**

1. 把新图片放到 `public/images/`，取新名字
2. 在 `src/pages/gallery.astro` 中找到旧条目的 `src`，改成新路径

---

### 3.4 新增一个视频

视频支持两种方式：**外链（推荐）** 和 **本地文件**。

#### 方式一：外链视频（B站）

把 `type` 设为 `'video'`，`src` 填 B站嵌入地址：

```js
{
  type: 'video',
  src: '//player.bilibili.com/player.html?bvid=BV1xx411x7xx',
  caption: 'Day 3 合唱练习片段',
  note: '分声部训练初见成效，和声效果出来了。',
},
```

> ⚠️ 外链视频的 `src` 是完整 URL，**不需要**加 `base` 前缀。

点击后 Lightbox 会弹出播放器。但 B站外链在 Lightbox 中可能受限——如果播放不流畅，改用下面方式。

#### 方式二：外链视频（其他平台 / 直接链接）

```js
{
  type: 'video',
  src: 'https://example.com/video.mp4',  // 直接视频文件链接
  caption: '合唱展演全程',
  note: '最后一天的展演，孩子们表现得特别好。',
},
```

#### 方式三：本地视频文件

1. 把 `.mp4` 视频复制到 `public/videos/` 文件夹
2. 填写路径：

```js
{
  type: 'video',
  src: base + 'videos/performance.mp4',
  caption: '合唱展演全程',
  note: '最后一天的展演。',
},
```

> 💡 视频的缩略图目前显示的是 `src` 指向的内容（外链视频会显示一个播放按钮覆盖层）。如果你想要自定义缩略图，可以先用一张截图作为 `type: 'image'`，然后等后续升级。

**改后效果**：时间线中出现带 ▶ 播放按钮的视频缩略图。点击 → Lightbox 弹出 → 自动播放。

---

### 3.5 修改注释文字

注释就是右侧那一列 hover 才展开的"感想"。修改非常简单：

找到对应条目，改 `note` 后面的文字：

```js
{
  type: 'image',
  src: base + 'images/day0-arrival.jpg',
  caption: '抵达澜沧拉祜族自治县',
  note: '把这段改成你想写的任何内容。支持中英文。',   // ← 只改这一行
},
```

如果不想显示注释（右侧空着），把 `note` 那行删掉即可。

---

### 3.6 新增一天的媒体组

如果某一天（比如 Day 3）有新的照片和视频，但画廊里还没有这天的日期组，需要新增一整组：

在 `galleryGroups` 数组里加一个新的日期组：

```js
const galleryGroups = [
  // ... 前面的日期组不动
  {
    date: '7月29日',
    items: [
      {
        type: 'image',
        src: base + 'images/day3-class.jpg',
        caption: '简谱教学课堂',
        note: '孩子们学简谱的速度比我们想象的快得多。',
      },
      {
        type: 'image',
        src: base + 'images/day3-practice.jpg',
        caption: '分声部练习',
        note: '第一次尝试三声部合唱，虽然有点乱，但大家都乐在其中。',
      },
    ],
  },    // ← 注意末尾的逗号
];
```

**验证**：日期组按你在数组中写的顺序排列，不是自动按日期排序。所以新增的放在最后就行。

---

### 3.7 图片和视频的两种引用方式

在画廊里，图片/视频的 `src` 有两种写法：

| 类型 | 写法 | 示例 |
|------|------|------|
| **本地文件** | `base + 'images/xxx.jpg'` | `base + 'images/day0-arrival.jpg'` |
| **本地视频** | `base + 'videos/xxx.mp4'` | `base + 'videos/performance.mp4'` |
| **外链视频** | 直接写 URL | `'//player.bilibili.com/player.html?bvid=...'` |
| **外链图片** | 直接写 URL | `'https://example.com/photo.jpg'` |

> 规则：**项目内的文件**（`public/images/`、`public/videos/` 下的）用 `base + '路径'`；**外部链接**直接写完整 URL。

---

## 四、修改页脚

**文件位置**：`src/components/Footer.astro`

页脚当前显示：队名、学校信息、GitHub 链接、版权声明。

**操作步骤**：

1. 打开 `src/components/Footer.astro`
2. 找到你想改的文字直接修改：

```astro
<!-- 比如把 GitHub 链接从 github.com 改成你的仓库地址 -->
<a href="https://github.com/Finderlzy/DianRhyme" target="_blank" rel="noopener noreferrer">
```

3. 保存

---

## 五、构建与推送

### 5.1 安装 Node.js（首次使用）

去 https://nodejs.org 下载 LTS 版本（长期支持版），双击安装，一路 Next 即可。

安装完成后，**重新打开终端**，验证：

```bash
node --version
npm --version
```

---

### 5.2 克隆项目到本地（首次使用）

```bash
git clone https://github.com/Finderlzy/DianRhyme.git
cd DianRhyme
npm install      # 安装依赖，约 1-2 分钟，只需执行一次
```

---

### 5.3 日常修改流程

这是你**每次改完内容后**的完整操作流程：

#### 第 1 步：构建

在项目根目录（`DianRhyme/`）下打开终端，运行：

```bash
npm run build
```

你会看到类似这样的输出：

```
15:59:01 [build] 3 page(s) built in 700ms
15:59:01 [build] Complete!
```

> 如果中间有红色报错，说明内容格式有问题。最常见的错误是 .md 文件的 frontmatter 写错了（漏了冒号、多打了换行）。对照已有的文件检查格式。

构建产物放在 `dist/` 文件夹中，这是最终要部署到网站上的文件。**你不需要手动操作 dist 文件夹**，GitHub Actions 会自动处理。

#### 第 2 步：本地预览（可选但推荐）

构建完先在本地看一眼，确认没问题再推送：

```bash
npm run dev
```

浏览器打开 `http://localhost:4321/DianRhyme/` 查看效果。注意本地预览地址和线上不同——本地是 `localhost:4321`，多了一层 `/DianRhyme/`。

确认无误后，按 `Ctrl+C` 停止开发服务器。

#### 第 3 步：提交到 Git

```bash
git add -A
git commit -m "update: 描述你改了什么"
```

例如：

```bash
git commit -m "update: 新增 Day 2 日记，替换 Day 0 照片"
```

`-m` 后面的引号里写一句话描述本次修改，方便以后回顾。

#### 第 4 步：推送到 GitHub

```bash
git push
```

#### 第 5 步：等待自动部署

推送后，GitHub Actions 自动开始构建和部署。你可以打开下面这个地址看进度：

**https://github.com/Finderlzy/DianRhyme/actions**

等最新的 workflow 变成绿色 ✓（通常 1-2 分钟），网站就更新了。

访问：**https://finderlzy.github.io/DianRhyme/**

> 如果部署完成后打开网站还是旧的，按 `Ctrl+Shift+R`（Windows）强制刷新，清除浏览器缓存。

---

### 5.4 完整命令速查

```bash
# 每次修改内容后，按顺序执行这 4 条：

npm run build                        # 1. 构建
npm run dev                          # 2. 预览（可选，Ctrl+C 停止）
git add -A                           # 3. 暂存所有改动
git commit -m "update: xxx"          # 4. 提交（引号里写描述）
git push                             # 5. 推送 → 等待 GitHub Actions 自动部署
```

---

## 六、常见问题

### Q1: `npm run build` 报错怎么办？

**不要慌。** 仔细看报错信息，它会告诉你哪个文件、第几行有问题。最常见原因：

- `.md` 文件的 frontmatter 里 `day:` 写了中文
- frontmatter 格式不对（`---` 没闭合）
- 图片 `src` 路径拼写错误

对照已有文件检查格式。

### Q2: 推送后网站没变化？

1. 先检查 GitHub Actions 是否成功：https://github.com/Finderlzy/DianRhyme/actions
2. 如果 Actions ✓ 了，强制刷新浏览器（`Ctrl+Shift+R`）
3. 如果还是旧的，等 2 分钟再刷（GitHub Pages 有缓存延迟）

### Q3: 本地预览和线上不一样？

本地 `npm run dev` 的 base 路径可能和线上不一致。确认效果以 `npm run build` 后的 `dist/` 目录为准。

### Q4: 图片放进去不显示？

检查：
1. 文件名拼写是否和代码里完全一致（大小写敏感！`day0.jpg` ≠ `Day0.jpg`）
2. 路径是否以 `/DianRhyme/` 开头
3. 图片是否确实放到了 `public/images/` 下面

### Q5: 想改网站配色？

打开 `src/styles/global.css`，找到 `:root` 里的颜色变量：

```css
--color-primary: #E8923A;       /* 主色：按钮、导航高亮 */
--color-bg: #FFF8EC;            /* 背景色：页面底色 */
--color-text: #3D3228;          /* 文字色：正文 */
--color-accent: #7BA587;        /* 点缀色：小装饰 */
```

改这四个颜色值，整个网站的配色方案就全换了。

### Q6: 不小心改坏了怎么办？

```bash
git status               # 看哪些文件被改了
git checkout -- 文件名    # 撤销单个文件的改动
git reset --hard HEAD    # 撤销所有改动的文件（慎用！会丢失未提交的修改）
```

---

*最后更新：2026-07-29*
