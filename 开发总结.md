# 开发总结 — 和音滇韵社会实践成果展示网页

## 项目概况

- **项目名称**：和音滇韵 — 社会实践成果展示网页
- **团队**：北京科技大学"酉良和音滇韵"实践团
- **技术栈**：Astro 4 + Markdown + GitHub Pages
- **成果**：3 个页面静态站点，内容通过 Markdown 文件维护

---

## 开发过程

### 1. 需求梳理

用户最初描述比较简洁——"做一个社会实践成果展示网页"。通过一对一问答，逐步明确了：

- **受众**：学校考核 + 对外宣传
- **内容板块**：首页概况、实践日记、图片视频（新闻稿不单独成页）
- **设计风格**：柔和、干净、简约，暖色调，呼应支教主题
- **交互细节**：
  - 日记页：时间线列表，点击后标题平滑居中，下方展开内容，其他条目下沉
  - 画廊页：三列布局（时间线-媒体-注释），hover 注释气泡展开，点击 Lightbox
- **技术偏好**：Astro + Markdown 内容管理，GitHub Pages 部署
- **内容编辑**：用户熟悉 Markdown，后续自己负责维护

### 2. 技术选型

**方案对比：**

| 方案 | 优点 | 缺点 |
|------|------|------|
| 纯静态 HTML + JS 数据文件 | 零依赖，编辑后直接生效 | 无组件化，重复代码多 |
| Vite + HTML/CSS/JS | 开发体验好，热更新 | 需要 Node.js，须构建 |
| **Astro 静态站点生成器** | ✅ Markdown 原生支持，组件化，构建产物小 | 需要 Node.js，须构建 |

用户选择 Astro——对其 Markdown 编辑方式最熟悉。

### 3. 实现过程

分 6 个阶段完成：

1. **项目初始化**：因 Node 20 不满足 create-astro 5.x 的要求（需 ≥22），改为手动搭建——手写 `package.json`、`astro.config.mjs`、`tsconfig.json`
2. **公共基础设施**：全局 CSS（暖色调主题）、BaseLayout（导航+页脚）、NavBar（固定顶部、滚动效果）
3. **首页**：Hero 区 → 四格信息卡片 → 关于实践文字 → 团队成员卡片
4. **实践日记页**：时间线列表 + CSS transition 展开/收起 + `Astro.glob` 读取 Markdown
5. **图片视频页**：三列网格画廊 + 气泡注释 hover + Lightbox（图片放大/视频播放/键盘导航）
6. **部署**：GitHub Actions + GitHub Pages

### 4. 遇到的问题及解决方案

#### 问题 1：Node.js 版本不兼容

**现象**：运行 `npm create astro@latest` 报错，提示需要 Node.js ≥ 22.12.0，而本地是 20.19.5。

**原因**：create-astro 5.x 升级了 Node 引擎要求。

**解决**：放弃脚手架，手动创建项目文件——`package.json`（指定 astro ^4.16.0）、`astro.config.mjs`、`tsconfig.json`，然后 `npm install`。Astro 4.x 适配 Node 20，功能完全满足需求。

---

#### 问题 2：Astro.glob 路径解析错误

**现象**：`npm run build` 时报 `AstroGlobNoMatch`，提示 `../content/diaries/*.md` 没有匹配到文件。

**原因**：`Astro.glob()` 的路径相对于**源文件所在位置**解析。`diary.astro` 在 `src/pages/`，而 content 在 `content/`，实际需要向上两级：`../../content/diaries/*.md`。

**解决**：将 `../content/diaries/*.md` 改为 `../../content/diaries/*.md`。

> **经验**：Astro 中 `Astro.glob` 的路径基准是当前 `.astro` 文件所在目录，不是项目根目录。

---

#### 问题 3：GitHub Pages 子路径部署后样式丢失 + 导航跳转失败

**现象**：部署到 `https://finderlzy.github.io/DianRhyme/` 后，首页只剩文字没有样式，导航栏点击跳转到 404。

**原因**：GitHub Pages 部署在子路径 `/DianRhyme/` 下，但所有资源路径和导航链接都硬编码为 `/`（根路径）。例如：
- CSS 引用 `/_astro/style.css` → 实际应该 `/DianRhyme/_astro/style.css`
- 导航链接 `/diary` → 实际应该 `/DianRhyme/diary`

浏览器在 `finderlzy.github.io` 根路径下找不到这些资源。

**解决**：

1. 在 `astro.config.mjs` 中设置 `base: '/DianRhyme/'`
2. NavBar 中使用 `import.meta.env.BASE_URL` 动态拼接路径
3. 画廊页图片引用同样使用 `BASE_URL` 前缀
4. 重新构建、提交、推送

**关键代码变更：**

```js
// astro.config.mjs
export default defineConfig({
  site: 'https://finderlzy.github.io',
  base: '/DianRhyme/',        // ← 关键配置
});

// NavBar.astro
const base = import.meta.env.BASE_URL;  // → '/DianRhyme/'
<a href={base}>首页</a>
<a href={base + 'diary'}>实践日记</a>
```

> **经验**：部署到 GitHub Pages 非 `username.github.io` 仓库时，必须设置 `base`，所有路径引用都要用 `import.meta.env.BASE_URL` 拼接，不能硬编码 `/`。

---

## 关键文件说明

| 文件/目录 | 用途 | 维护频率 |
|-----------|------|----------|
| `content/diaries/*.md` | 日记内容 | 每新增一天 |
| `content/overview.md` | 首页概况 | 偶尔 |
| `content/team.md` | 团队信息 | 极少 |
| `src/pages/gallery.astro` | 画廊数据（图片/视频列表） | 新增照片时 |
| `public/images/` | 图片文件 | 新增照片时 |
| `astro.config.mjs` | 站点配置（base 路径等） | 极少 |
| `.github/workflows/deploy.yml` | 自动化部署 | 极少 |

---

## 部署步骤（备忘）

```bash
# 1. 修改内容（Markdown / 图片）
# 2. 构建
npm run build
# 3. 提交并推送
git add -A
git commit -m "update: xxx"
git push
# 4. GitHub Actions 自动部署，等 1-2 分钟即可
```

---

## 待补充内容

- [ ] 更多实践日记（Day 3 ~ Day 12）
- [ ] 替换画廊页占位图片为真实照片
- [ ] 添加视频链接（B站/YouTube）
- [ ] 团队合影替换 Hero 区占位图
- [ ] 成员头像照片

---

*开发日期：2026-07-29*
